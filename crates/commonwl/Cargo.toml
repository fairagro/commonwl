[package]
name = "commonwl"
version.workspace = true
edition.workspace = true
repository.workspace = true
license.workspace = true

[dependencies]
cwl_core = { path = "../core" }
cwl_engine = { path = "../engine", optional = true }

[features]
default = []
engine = ["cwl_engine"]

[lib]
path = "lib.rs"