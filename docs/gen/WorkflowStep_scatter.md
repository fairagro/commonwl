Specifies one or more input parameters to scatter.  Each listed parameter becomes an array for the step, and repeated parameters create nested arrays.

If any scattered parameter value is an empty array, all outputs are set to empty arrays and no work is performed for the step.
